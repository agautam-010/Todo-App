import {
  Alert,
  TextInput,
  Pressable,
  View,
  TouchableOpacity,
  ActivityIndicator,
  Switch,
} from 'react-native';

import {Container, Wrapper, Row, Card} from './Containers';
import Typography from './Typography';
import {FlatList} from './EnhancedComponent';
import Input from './Input';
import Button from './Button';

export {
  Alert,
  TextInput,
  View,
  Pressable,
  TouchableOpacity,
  ActivityIndicator,
  Switch,
  Container,
  Wrapper,
  Row,
  Card,
  Typography,
  FlatList,
  Input,
  Button,
};
